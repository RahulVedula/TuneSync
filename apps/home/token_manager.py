import requests
import time

#This code has been deleted for privacy reasons, however the token manager takes a spotify refresh token and uses it to find a spotify access token every hour (the time limit on access tokens is one hour)
